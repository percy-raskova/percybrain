-- Transparent.nvim: Terminal transparency support
-- Lazy-loaded: Optional visual enhancement, loads on demand
return {
  "xiyaowong/transparent.nvim",
  lazy = true,
  cmd = { "TransparentEnable", "TransparentDisable", "TransparentToggle" },
}
