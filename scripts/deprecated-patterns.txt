# OVIWrite Deprecated API Patterns
#
# Format: PATTERN|REPLACEMENT|SEVERITY
# SEVERITY: ERROR (blocks commit), WARNING (notifies only)
#
# Add new patterns as APIs are deprecated in Neovim updates
#

# Neovim 0.9+ API changes
vim\.highlight\.on_yank|vim.hl.on_yank|ERROR

# lazy.nvim plugin spec patterns
config\s*=\s*\{\}|opts = {}|WARNING
require\s*\(\s*["']nvim-treesitter\.configs["']\s*\)\.setup|Use opts = {} in plugin spec|WARNING

# Removed/deprecated functions
setup_ts_grammar|[REMOVED - Delete this line]|ERROR
vim\.lsp\.buf\.range_formatting|vim.lsp.buf.format|WARNING

# Deprecated LSP config patterns (mason-lspconfig)
on_attach\s*=\s*function|Use opts with on_attach|WARNING

# Add more patterns as discovered during development
